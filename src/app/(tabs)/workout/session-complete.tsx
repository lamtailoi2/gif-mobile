import React from "react";
import SessionComplete from "@/features/workout-session/components/session-complete";

export default function WorkoutSessionCompleteScreen() {
  return <SessionComplete />;
}
